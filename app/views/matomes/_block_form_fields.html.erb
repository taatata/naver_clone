<%= form_for(@matome, remote: true) do |m| %>
  <%= m.text_field :title %>
  <%= m.text_field :content %>

  <%= render 'blocks/form_fields', m: m, name: 'midasis' %>

  <%= m.submit "保存" %>
<% end %>

