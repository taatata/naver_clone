<div class="check <%= name %>">
  <%= render "#{name}/check" %>
</div>

<div class="form <%= 'hide' if name.match(/links|mimages|movies/) %>">
  <%= form_for(@matome, remote: false) do |m| %>
    <%= render 'blocks/form_fields', m: m, name: name %>
    <%= m.submit "保存" %>
  <% end %>
</div>
